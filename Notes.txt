Common DocmosisCloudRequest and Response object:

TemplateListRequest (com.docmosis.sdk.template)
 extends DocmosisCloudRequest (com.docmosis.sdk.request)
    String url
    String accessKey;


	
Template.list() - returns a TemplateListRequest.
  templateListRequest.setEnv(url, accessKey);

   TemplateDetailsResult templates = Template.list().execute(optional url and key);
   List<TemplateDetails> list = templates.list();
   for(TemplateDetails td : list) {
      ...
   }

Upload - templateFile, Name, Description,devmode, keepPrev, delims,normalize.


Method chaining:

TemplateUploadRequest
   file(File)
   name(String name)
   devMode(boolean)
   keepPrev(boolean)
   ...

Template.upload().file(File).execute() //Simplest form

Template.get().name().execute();
 returns TemplateDetailsResult?
 
 
 See ChargeBee API
 
 Will return correct object in eg .url(inurl)??